export type arrayDaAlegria = {
    numero: number[],
    quantidade: number,
    impares: number,
    soma: number,
};


const resultado: arrayDaAlegria = {
    numero: [],
    quantidade: (numero: number) => Array.map([])
    },
    impares: (numero: number) => {
        Array.forEach(numero % 2 ===0 )
    },
    soma: (numero: number)
}


console.log(resultado)